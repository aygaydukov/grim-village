# Архитектура

## Принцип

Симуляция не знает про Canvas. Рендер читает состояние мира и рисует. Позже можно заменить `src/render` на PixiJS или спрайты без переписывания поведения агентов.

```
src/
  main.ts          — игровой цикл, ввод, связь слоёв
  sim/             — мир, карта, агенты, поведение
  render/          — камера, палитра, отрисовка
  input/           — клавиатура
  ui/              — HUD / инспектор (DOM)
```

## Тик мира

- ~30 тиков/сек при скорости ×1 (`TICKS_PER_REAL_SECOND`)
- `dayLength = 1200` тиков ≈ один игровой день/ночь (~40 с реала при ×1)
- **Календарь лет** (`src/sim/time.ts`): **3 года возраста за 1 реальные сутки** при ×1
  - год = 720 игровых дней; сезон = 180 дней
  - беременность ≈ 9 мес.; пауза после родов ≈ 1 год
- Ночь усиливает холод/усталость, еда почти не растёт

> Длина `dayLength` специально короткая (голод/работа), а «годы» замедлены отдельно — иначе сутки реала давали бы сотни лет возраста.

## Агенты

Нужды: `hunger`, `energy`.  
Семья: `spouseId`, `motherId`, `fatherId`.  
Переноска: `carriedFood` → амбар.

Состояния: wander, seekFood, eat, seekRest, sleep, seekMate, court, seekGather, gather, deposit, idle.

Смерть: голод, холод/истощение ночью, старость.  
Рождение: ухаживание → супруги → беременность → ребёнок с родителями.

## Карта

Процедурная (seed): трава, грязь, лес, вода, хижины, **амбар** в центре.  
Центральная площадь (радиус ~7) — только грязь/амбар/хижины, без воды.  
Тропы `carvePath` от каждой хижины к амбару.

## Профессии и задачи

Модуль `src/sim/jobs.ts`:
- профессии: child / gatherer / laborer / keeper / elder
- задачи: idle, patrol, returnHome, gather, deposit, eat, rest, social, play, **build**
- состояния: …, **seekBuild**, **build**
- leash: агент не уходит далеко от дома (или амбара для сборщика/сторожа)

Приоритет поведения: нужды → возврат на участок → работа по профессии.
Бесцельного бродяжничества по карте нет.

## Рынок труда

Раз в игровой день (`rebalanceVillageLabor` в `jobs.ts`):
- квоты сборщиков / сторожей / батраков зависят от запасов амбара;
- взрослые перераспределяются по «пригодности» (силы, голод, беременность).

## История и модуляция

- `dayHistory` — до 30 снимков (`history.ts`): население, амбар, дикая еда, профессии, **дельты дня** (`birthsToday`, `deathsToday`), **сезон**, **highHunger**, **именованные события** (`events`).
- `events.ts` — буфер событий дня: рождения, смерти (с причиной), смена профессий при `rebalanceVillageLabor`.
- `chronicle.ts` — текстовые записи летописи для UI (имена, не только счётчики).
- `persist.ts` — сериализация `World` в JSON, localStorage, восстановление PRNG state.
- `season.ts` — цикл весна/лето/осень/зима (8 дней), множитель regen еды.
- `shocks.ts` — редкий сезонный шок «неурожай» (осень/зима, низкий амбар): множитель regen 0.52–0.60 на 2–4 дня, запись в летопись.
- `pathfind.ts` — A* по сетке (8-связность), кэш waypoints на агента; при блокировке прямого шага в `moveToward`.
- `housing.ts` — постройка хижин батраками при перенаселении, визуал стройплощадки в `render/renderer.ts`.
- `government.ts` — десятина 10%, казна старосты, очередь на еду, субсидии амбара и стройки, назначение старосты.
- `migration.ts` — исход семей при критическом перенаселении и голоде; кулдаун, защита старосты/последнего сторожа.
- `src/version.ts` — `GAME_VERSION`, `CHANGELOG` для UI.
- Headless-прогон: `npm run simulate` (10 дней по умолчанию), `npm test` — smoke на нескольких seed + roundtrip save/load.
- Модуль `modulate.ts`: критерии стабильности для CI и cloud-агента.
- Витрина: `npm run village:status` → `docs/status.json` + `docs/index.html` (GitHub Pages chronicle).
- Headless: `npm run village:daemon` — автосейв, версии поселения, SVG-снимки (`scripts/settlement-*.ts`).
- Релизы игры: только вкладка «История версий» (`src/version.ts`).

## Дальнейший апгрейд графики

1. Оставить `World` / `Agent` как есть.
2. Подменить `renderWorld` (Pixi / спрайт-атлас).
3. Камеру и `screenToWorld` сохранить или адаптировать один раз.
